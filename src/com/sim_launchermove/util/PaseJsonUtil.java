package com.sim_launchermove.util;

import java.util.ArrayList;
import java.util.List;

import com.sim_launchermove.model.CarModel;

public class PaseJsonUtil {

	public static List<CarModel> carListData(Object dataResult){
		List<CarModel> list = new ArrayList<CarModel>();
		if(null != dataResult && !"null".equals(dataResult) && !"".equals(dataResult)){
			
		
		}
		return list;
	}
}
